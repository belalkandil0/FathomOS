<UserControl x:Class="FathomOS.Modules.ProjectManagement.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Projects" FontSize="24" FontWeight="Light"/>
            <TextBlock Text="View and manage all survey projects"
                       FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Search and Filter Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox" Grid.Column="0"
                     Padding="8" Margin="0,0,10,0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Tag="Search by project name, number, or client..."/>

            <ComboBox x:Name="StatusFilter" Grid.Column="1"
                      Width="130" Margin="0,0,10,0"/>

            <ComboBox x:Name="ClientFilter" Grid.Column="2"
                      Width="180" Margin="0,0,10,0"
                      DisplayMemberPath="Name"/>

            <Button x:Name="AddButton" Grid.Column="3"
                    Content="+ New Project" Padding="15,8"
                    Click="AddButton_Click"/>
        </Grid>

        <!-- Project List -->
        <DataGrid x:Name="ProjectsGrid" Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  MouseDoubleClick="ProjectsGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Project #" Binding="{Binding ProjectNumber}" Width="100"/>
                <DataGridTextColumn Header="Project Name" Binding="{Binding ProjectName}" Width="200"/>
                <DataGridTextColumn Header="Client" Binding="{Binding Client.Name}" Width="150"/>
                <DataGridTextColumn Header="Type" Binding="{Binding ProjectType}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                <DataGridTextColumn Header="Phase" Binding="{Binding CurrentPhase}" Width="90"/>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat='{}{0:dd MMM yyyy}'}" Width="100"/>
                <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat='{}{0:dd MMM yyyy}'}" Width="100"/>
                <DataGridTextColumn Header="% Complete" Binding="{Binding PercentComplete, StringFormat='{}{0:F0}%'}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock x:Name="StatusText" Text="0 projects" VerticalAlignment="Center"/>
                <TextBlock Text=" | " Foreground="Gray" VerticalAlignment="Center"/>
                <TextBlock x:Name="ActiveText" Text="0 active" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="ViewButton" Content="View" Padding="15,5" Margin="0,0,5,0"
                        Click="ViewButton_Click"/>
                <Button x:Name="EditButton" Content="Edit" Padding="15,5" Margin="0,0,5,0"
                        Click="EditButton_Click"/>
                <Button x:Name="ReportButton" Content="Report" Padding="15,5"
                        Click="ReportButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
