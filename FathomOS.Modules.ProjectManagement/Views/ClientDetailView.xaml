<UserControl x:Class="FathomOS.Modules.ProjectManagement.Views.ClientDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="650">

    <UserControl.Resources>
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>

        <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="FieldComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="20" FontWeight="Light" Margin="0,0,0,20"/>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: Company Name / Short Name -->
                <StackPanel Grid.Row="0" Grid.Column="0">
                    <TextBlock Text="Company Name *" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="2">
                    <TextBlock Text="Short Name" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding ShortName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>

                <!-- Row 1: Client Code / Type -->
                <StackPanel Grid.Row="1" Grid.Column="0">
                    <TextBlock Text="Client Code" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding ClientCode, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="2">
                    <TextBlock Text="Client Type" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox ItemsSource="{Binding ClientTypes}"
                              SelectedItem="{Binding SelectedClientType}"
                              Style="{StaticResource FieldComboBoxStyle}"/>
                </StackPanel>

                <!-- Row 2: Industry / Currency -->
                <StackPanel Grid.Row="2" Grid.Column="0">
                    <TextBlock Text="Industry" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding Industry, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="2">
                    <TextBlock Text="Currency" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox ItemsSource="{Binding Currencies}"
                              SelectedItem="{Binding SelectedCurrency}"
                              Style="{StaticResource FieldComboBoxStyle}"/>
                </StackPanel>

                <!-- Row 3: Email / Phone -->
                <StackPanel Grid.Row="3" Grid.Column="0">
                    <TextBlock Text="Email" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="2">
                    <TextBlock Text="Phone" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>

                <!-- Row 4: Website -->
                <StackPanel Grid.Row="4" Grid.ColumnSpan="3">
                    <TextBlock Text="Website" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding Website, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>

                <!-- Row 5: Address -->
                <StackPanel Grid.Row="5" Grid.Column="0">
                    <TextBlock Text="Address Line 1" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding AddressLine1, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="5" Grid.Column="2">
                    <TextBlock Text="Address Line 2" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding AddressLine2, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>

                <!-- Row 6: City / State -->
                <StackPanel Grid.Row="6" Grid.Column="0">
                    <TextBlock Text="City" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="6" Grid.Column="2">
                    <TextBlock Text="State/Province" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding State, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>

                <!-- Row 7: Postal Code / Country -->
                <StackPanel Grid.Row="7" Grid.Column="0">
                    <TextBlock Text="Postal Code" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding PostalCode, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="7" Grid.Column="2">
                    <TextBlock Text="Country" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>

                <!-- Row 8: Payment Terms / Active -->
                <StackPanel Grid.Row="8" Grid.Column="0">
                    <TextBlock Text="Payment Terms (Days)" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Text="{Binding PaymentTermsDays, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBoxStyle}"/>
                </StackPanel>
                <StackPanel Grid.Row="8" Grid.Column="2" Margin="0,20,0,0">
                    <CheckBox Content="Active" IsChecked="{Binding IsActive}"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Padding="20,8" Margin="0,0,10,0"
                    Command="{Binding SaveCommand}"
                    Background="#2196F3" Foreground="White" BorderThickness="0"/>
            <Button Content="Cancel" Padding="20,8"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
