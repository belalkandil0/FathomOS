<UserControl x:Class="FathomOS.Modules.PersonnelManagement.Views.TimesheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Timesheets" FontSize="24" FontWeight="Light"/>
            <TextBlock Text="Manage timesheet entries and approval workflow"
                       FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Filter Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,15,0">
                <TextBlock Text="Personnel:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="PersonnelFilter" Width="200" DisplayMemberPath="FullName"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,15,0">
                <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="StatusFilter" Width="120"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,15,0">
                <TextBlock Text="Period:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker x:Name="PeriodStartPicker" Width="110" Margin="0,0,5,0"/>
                <TextBlock Text="to" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker x:Name="PeriodEndPicker" Width="110"/>
            </StackPanel>

            <Button x:Name="NewTimesheetButton" Grid.Column="4" Content="+ New Timesheet"
                    Padding="15,8" Click="NewTimesheetButton_Click"/>
        </Grid>

        <!-- Timesheet List -->
        <DataGrid x:Name="TimesheetsGrid" Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  MouseDoubleClick="TimesheetsGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timesheet #" Binding="{Binding TimesheetNumber}" Width="120"/>
                <DataGridTextColumn Header="Personnel" Binding="{Binding Personnel.FullName}" Width="150"/>
                <DataGridTextColumn Header="Period" Binding="{Binding PeriodDisplay}" Width="150"/>
                <DataGridTextColumn Header="Vessel" Binding="{Binding VesselName}" Width="130"/>
                <DataGridTextColumn Header="Project" Binding="{Binding ProjectName}" Width="130"/>
                <DataGridTextColumn Header="Regular Hrs" Binding="{Binding TotalRegularHours, StringFormat='{}{0:F1}'}" Width="80"/>
                <DataGridTextColumn Header="OT Hrs" Binding="{Binding TotalOvertimeHours, StringFormat='{}{0:F1}'}" Width="70"/>
                <DataGridTextColumn Header="Total Hrs" Binding="{Binding TotalHours, StringFormat='{}{0:F1}'}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusDisplay}" Width="120"/>
                <DataGridTextColumn Header="Submitted" Binding="{Binding SubmittedAt, StringFormat='{}{0:dd MMM yyyy}'}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock x:Name="StatusText" Text="0 timesheets" VerticalAlignment="Center"/>
                <TextBlock Text=" | " Foreground="Gray" VerticalAlignment="Center"/>
                <TextBlock x:Name="PendingText" Text="0 pending approval" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="ViewButton" Content="View" Padding="15,5" Margin="0,0,5,0"
                        Click="ViewButton_Click"/>
                <Button x:Name="ApproveButton" Content="Approve" Padding="15,5" Margin="0,0,5,0"
                        Click="ApproveButton_Click"/>
                <Button x:Name="RejectButton" Content="Reject" Padding="15,5" Margin="0,0,5,0"
                        Click="RejectButton_Click"/>
                <Button x:Name="ExportButton" Content="Export" Padding="15,5"
                        Click="ExportButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
