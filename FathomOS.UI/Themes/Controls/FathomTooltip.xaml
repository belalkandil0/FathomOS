<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================== -->
    <!-- FathomOS Design System - FathomTooltip        -->
    <!-- Owned by: UI-AGENT                            -->
    <!-- DO NOT modify without UI-AGENT approval       -->
    <!-- ============================================== -->

    <!-- Default Tooltip Style -->
    <Style x:Key="FathomTooltip" TargetType="{x:Type ToolTip}">
        <Setter Property="FontFamily" Value="{DynamicResource FontPrimary}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="#FFFFFF"/>
        <Setter Property="Background" Value="#1A1A1D"/>
        <Setter Property="BorderBrush" Value="#2D2D30"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="MaxWidth" Value="300"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToolTip}">
                    <Border x:Name="TooltipBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">

                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="2"
                                              BlurRadius="8"
                                              Opacity="0.4"
                                              Direction="270"/>
                        </Border.Effect>

                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center">
                            <ContentPresenter.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                </Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Light Theme Tooltip -->
    <Style x:Key="FathomTooltipLight" TargetType="{x:Type ToolTip}" BasedOn="{StaticResource FathomTooltip}">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    </Style>

    <!-- Primary Tooltip -->
    <Style x:Key="FathomTooltipPrimary" TargetType="{x:Type ToolTip}" BasedOn="{StaticResource FathomTooltip}">
        <Setter Property="Foreground" Value="{DynamicResource TextOnPrimaryBrush}"/>
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
    </Style>

    <!-- Success Tooltip -->
    <Style x:Key="FathomTooltipSuccess" TargetType="{x:Type ToolTip}" BasedOn="{StaticResource FathomTooltip}">
        <Setter Property="Foreground" Value="#FFFFFF"/>
        <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}"/>
    </Style>

    <!-- Warning Tooltip -->
    <Style x:Key="FathomTooltipWarning" TargetType="{x:Type ToolTip}" BasedOn="{StaticResource FathomTooltip}">
        <Setter Property="Foreground" Value="#1A1A1D"/>
        <Setter Property="Background" Value="{DynamicResource WarningBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource WarningBrush}"/>
    </Style>

    <!-- Error Tooltip -->
    <Style x:Key="FathomTooltipError" TargetType="{x:Type ToolTip}" BasedOn="{StaticResource FathomTooltip}">
        <Setter Property="Foreground" Value="#FFFFFF"/>
        <Setter Property="Background" Value="{DynamicResource ErrorBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}"/>
    </Style>

    <!-- Default Style for all Tooltips in the app -->
    <Style TargetType="{x:Type ToolTip}" BasedOn="{StaticResource FathomTooltip}"/>

</ResourceDictionary>
