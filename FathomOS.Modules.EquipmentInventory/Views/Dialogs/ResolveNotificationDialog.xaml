<mah:MetroWindow x:Class="FathomOS.Modules.EquipmentInventory.Views.Dialogs.ResolveNotificationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Resolve Notification"
        Height="320" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        TitleCharacterCasing="Normal"
        BorderThickness="1"
        BorderBrush="{DynamicResource SuccessBrush}"
        GlowBrush="{DynamicResource SuccessBrush}">
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <iconPacks:PackIconMaterial Kind="CheckCircleOutline" Width="28" Height="28" 
                                            Foreground="{DynamicResource SuccessBrush}" 
                                            VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="Resolve Notification" Style="{DynamicResource HeadingMedium}"/>
            </StackPanel>
            <TextBlock Text="Add notes about how this issue was resolved (optional)." 
                       Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13" TextWrapping="Wrap"/>
        </StackPanel>
        
        <!-- Resolution Notes -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Resolution Notes" FontSize="12" FontWeight="SemiBold" 
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
            <TextBox x:Name="NotesBox" Height="100" TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     mah:TextBoxHelper.Watermark="Describe how the issue was resolved..."
                     VerticalContentAlignment="Top"/>
        </StackPanel>
        
        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="1" Content="Cancel" Style="{DynamicResource SecondaryButton}"
                    Click="Cancel_Click" Padding="20,10" Margin="0,0,10,0"/>
            <Button Grid.Column="2" Style="{DynamicResource PrimaryButton}"
                    Click="Resolve_Click" Padding="20,10">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Check" Width="16" Height="16" 
                                                VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Mark as Resolved"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</mah:MetroWindow>
