<mah:MetroWindow x:Class="FathomOS.Modules.EquipmentInventory.Views.Dialogs.DuplicateEquipmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Duplicate Equipment"
        Height="420" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        TitleCharacterCasing="Normal"
        BorderThickness="1"
        BorderBrush="{DynamicResource AccentBrush}"
        GlowBrush="{DynamicResource AccentBrush}">
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <iconPacks:PackIconMaterial Kind="ContentCopy" Width="28" Height="28" 
                                            Foreground="{DynamicResource AccentBrush}" 
                                            VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock Text="Duplicate Equipment" Style="{DynamicResource HeadingMedium}"/>
            </StackPanel>
            <TextBlock Text="Create copies of the selected equipment." 
                       Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13"/>
        </StackPanel>
        
        <!-- Options -->
        <StackPanel Grid.Row="1">
            <!-- Source Info -->
            <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="SOURCE EQUIPMENT" FontSize="11" FontWeight="Bold" 
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                    <TextBlock x:Name="SourceName" FontSize="14" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock x:Name="SourceAsset" FontSize="12" FontFamily="Consolas"
                               Foreground="{DynamicResource AccentBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- Number of Copies -->
            <TextBlock Text="Number of Copies" FontSize="12" FontWeight="SemiBold" 
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
            <mah:NumericUpDown x:Name="CopyCountBox" Value="1" Minimum="1" Maximum="100" 
                               Margin="0,0,0,20"/>
            
            <!-- Copy Options -->
            <TextBlock Text="COPY OPTIONS" FontSize="11" FontWeight="Bold" 
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,12"/>
            
            <CheckBox x:Name="CopySerialCheck" Content="Copy serial number (may cause duplicates)" 
                      Margin="0,0,0,8" IsChecked="False"/>
            <CheckBox x:Name="CopyPurchaseCheck" Content="Copy purchase information" 
                      Margin="0,0,0,8" IsChecked="False"/>
            <CheckBox x:Name="CopyPhotosCheck" Content="Copy photos" 
                      Margin="0,0,0,8" IsChecked="False"/>
            <CheckBox x:Name="CopyDocsCheck" Content="Copy documents" 
                      Margin="0,0,0,8" IsChecked="False"/>
        </StackPanel>
        
        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="1" Content="Cancel" Style="{DynamicResource SecondaryButton}"
                    Click="Cancel_Click" Padding="20,10" Margin="0,0,10,0"/>
            <Button Grid.Column="2" Style="{DynamicResource PrimaryButton}"
                    Click="Duplicate_Click" Padding="20,10">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="ContentCopy" Width="16" Height="16" 
                                                VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Duplicate"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</mah:MetroWindow>
