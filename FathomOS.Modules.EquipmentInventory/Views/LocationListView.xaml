<UserControl x:Class="FathomOS.Modules.EquipmentInventory.Views.LocationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:converters="clr-namespace:FathomOS.Modules.EquipmentInventory.Converters">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <converters:InverseNullToVisibilityConverter x:Key="InverseNullToVisibility"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel>
                <TextBlock Text="Locations" Style="{DynamicResource HeadingLarge}" Margin="0,0,0,5"/>
                <TextBlock Text="Manage bases, vessels, and storage locations" 
                           Foreground="{DynamicResource TextSecondaryBrush}" FontSize="14"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{DynamicResource SecondaryButton}" Margin="0,0,10,0"
                        Command="{Binding RefreshCommand}" ToolTip="Refresh">
                    <iconPacks:PackIconMaterial Kind="Refresh" Width="16" Height="16"/>
                </Button>
                <Button Style="{DynamicResource PrimaryButton}" Command="{Binding AddLocationCommand}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Plus" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Add Location"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>
            
            <!-- Locations List -->
            <Border Grid.Column="0" Style="{DynamicResource CardBorder}" Margin="0,0,15,0" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Search Bar -->
                    <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="15" 
                            CornerRadius="12,12,0,0">
                        <Border Background="{DynamicResource CardBrush}" CornerRadius="8" Padding="12,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <iconPacks:PackIconMaterial Kind="Magnify" Width="16" Height="16" 
                                                            Foreground="{DynamicResource TextSecondaryBrush}" 
                                                            VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                                         Background="Transparent" BorderThickness="0" FontSize="13"
                                         Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"
                                         mah:TextBoxHelper.Watermark="Search locations..."/>
                            </Grid>
                        </Border>
                    </Border>
                    
                    <!-- Count -->
                    <Border Grid.Row="1" Background="{DynamicResource BackgroundBrush}" Padding="15,10">
                        <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13">
                            <Run Text="{Binding FilteredLocations.Count, Mode=OneWay}"/>
                            <Run Text="locations"/>
                        </TextBlock>
                    </Border>
                    
                    <!-- Locations List -->
                    <ListBox Grid.Row="2" ItemsSource="{Binding FilteredLocations}" 
                             SelectedItem="{Binding SelectedLocation}"
                             Background="Transparent" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="15,12"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="border" Background="Transparent" 
                                                    BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderBrush}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource CardBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Location Icon -->
                                    <Border Width="44" Height="44" CornerRadius="10" Margin="0,0,15,0"
                                            Background="{DynamicResource SurfaceBrush}">
                                        <iconPacks:PackIconMaterial Kind="MapMarker" Width="22" Height="22"
                                                                    Foreground="{DynamicResource AccentBrush}"
                                                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding Code}" FontSize="12" FontFamily="Consolas"
                                                       Foreground="{DynamicResource AccentBrush}" Margin="0,0,15,0"/>
                                            <TextBlock Text="{Binding Type}" FontSize="12"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </StackPanel>
                                    
                                    <!-- Active Indicator -->
                                    <Border Grid.Column="2" Width="8" Height="8" CornerRadius="4" 
                                            VerticalAlignment="Center"
                                            Background="{Binding IsActive, Converter={StaticResource BoolToVisibility}, 
                                                        ConverterParameter=#00D4AA, FallbackValue=#888888}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#00D4AA"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Background" Value="#888888"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
            
            <!-- Details Panel -->
            <Border Grid.Column="1" Style="{DynamicResource CardBorder}" Padding="0">
                <Grid>
                    <!-- Location Details -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                  Visibility="{Binding SelectedLocation, Converter={StaticResource NullToVisibility}}">
                        <StackPanel>
                            <!-- Header -->
                            <Border Background="{DynamicResource SurfaceBrush}" Padding="20" CornerRadius="12,12,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Width="56" Height="56" CornerRadius="12" Margin="0,0,15,0">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#00D4AA" Offset="0"/>
                                                <GradientStop Color="#00B4D8" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <iconPacks:PackIconMaterial Kind="MapMarker" Width="26" Height="26"
                                                                    Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding SelectedLocation.Name}" FontSize="18" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding SelectedLocation.Code}" FontSize="14" FontFamily="Consolas"
                                                   Foreground="{DynamicResource AccentBrush}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- Details -->
                            <StackPanel Margin="20">
                                <TextBlock Text="TYPE" FontSize="10" FontWeight="SemiBold" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" Opacity="0.7" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding SelectedLocation.Type}" FontSize="14" 
                                           Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,15"/>
                                
                                <TextBlock Text="DESCRIPTION" FontSize="10" FontWeight="SemiBold" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" Opacity="0.7" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding SelectedLocation.Description, FallbackValue='No description'}" 
                                           FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" 
                                           TextWrapping="Wrap" Margin="0,0,0,15"/>
                                
                                <TextBlock Text="ADDRESS" FontSize="10" FontWeight="SemiBold" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" Opacity="0.7" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding SelectedLocation.Address, FallbackValue='-'}" 
                                           FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" 
                                           TextWrapping="Wrap" Margin="0,0,0,15"/>
                                
                                <TextBlock Text="CONTACT" FontSize="10" FontWeight="SemiBold" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" Opacity="0.7" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding SelectedLocation.ContactPerson, FallbackValue='-'}" 
                                           FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,15"/>
                                
                                <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,5,0,20"/>
                                
                                <!-- Action Buttons -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Button Grid.Column="0" Style="{DynamicResource PrimaryButton}" 
                                            Command="{Binding EditLocationCommand}" Margin="0,0,5,0">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Pencil" Width="14" Height="14" Margin="0,0,8,0"/>
                                            <TextBlock Text="Edit"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <Button Grid.Column="1" Style="{DynamicResource SecondaryButton}" 
                                            Command="{Binding DeleteLocationCommand}" Margin="5,0,0,0"
                                            Foreground="#E53935">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Delete" Width="14" Height="14" Margin="0,0,8,0"/>
                                            <TextBlock Text="Delete"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                    
                    <!-- Empty State -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                Visibility="{Binding SelectedLocation, Converter={StaticResource InverseNullToVisibility}}">
                        <Border Width="80" Height="80" CornerRadius="40" Background="{DynamicResource SurfaceBrush}"
                                HorizontalAlignment="Center" Margin="0,0,0,20">
                            <iconPacks:PackIconMaterial Kind="MapMarker" Width="36" Height="36" 
                                                        Foreground="{DynamicResource TextSecondaryBrush}" 
                                                        HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                        </Border>
                        <TextBlock Text="Select Location" FontSize="16" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextSecondaryBrush}" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Click a location to view details" FontSize="13"
                                   Foreground="{DynamicResource TextSecondaryBrush}" 
                                   HorizontalAlignment="Center" Margin="0,8,0,0" Opacity="0.7"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
