<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Gradient Theme - Professional gradient backgrounds with depth -->
    
    <!-- Primary Colors -->
    <Color x:Key="PrimaryColor">#6366F1</Color>
    <Color x:Key="PrimaryDarkColor">#4F46E5</Color>
    <Color x:Key="PrimaryLightColor">#A5B4FC</Color>
    <Color x:Key="AccentColor">#EC4899</Color>
    <Color x:Key="AccentLightColor">#F472B6</Color>
    
    <!-- Background Gradient Colors -->
    <Color x:Key="GradientStart">#1E1B4B</Color>
    <Color x:Key="GradientMiddle">#312E81</Color>
    <Color x:Key="GradientEnd">#4338CA</Color>
    
    <!-- Surface Colors -->
    <Color x:Key="SurfaceColor">#1E293B</Color>
    <Color x:Key="SurfaceLightColor">#334155</Color>
    <Color x:Key="CardColor">#1E293B</Color>
    
    <!-- Text Colors -->
    <Color x:Key="PrimaryTextColor">#F8FAFC</Color>
    <Color x:Key="SecondaryTextColor">#94A3B8</Color>
    <Color x:Key="DisabledTextColor">#64748B</Color>
    
    <!-- Border Colors -->
    <Color x:Key="BorderColor">#475569</Color>
    <Color x:Key="DividerColor">#334155</Color>
    
    <!-- Gradient Brushes -->
    <LinearGradientBrush x:Key="WindowBackgroundGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource GradientStart}" Offset="0"/>
        <GradientStop Color="{StaticResource GradientMiddle}" Offset="0.5"/>
        <GradientStop Color="{StaticResource GradientEnd}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="0"/>
        <GradientStop Color="{StaticResource PrimaryColor}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
        <GradientStop Color="{StaticResource AccentLightColor}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="ButtonGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
        <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
    </LinearGradientBrush>
    
    <!-- Solid Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceLightBrush" Color="{StaticResource SurfaceLightColor}"/>
    <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}"/>
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="{StaticResource PrimaryTextColor}"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}"/>
    <SolidColorBrush x:Key="DisabledTextBrush" Color="{StaticResource DisabledTextColor}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="DividerBrush" Color="{StaticResource DividerColor}"/>
    
    <!-- Window -->
    <Brush x:Key="WindowBackground">{StaticResource WindowBackgroundGradient}</Brush>
    <SolidColorBrush x:Key="WindowForeground" Color="{StaticResource PrimaryTextColor}"/>
    
    <!-- Panel Backgrounds -->
    <SolidColorBrush x:Key="PanelBackground" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SidebarBackground" Color="#0F172A"/>
    <Brush x:Key="ToolbarBackground">{StaticResource HeaderGradient}</Brush>
    
    <!-- Button Styles -->
    <Style x:Key="GradientButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonGradient}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.3" Color="{StaticResource PrimaryColor}"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#818CF8" Offset="0"/>
                                        <GradientStop Color="{StaticResource PrimaryColor}" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource DisabledTextBrush}"/>
                            <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- TextBox Style -->
    <Style x:Key="GradientTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CaretBrush" Value="{StaticResource PrimaryTextBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Card Style with Glow -->
    <Style x:Key="GlowCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2" Color="{StaticResource PrimaryColor}"/>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- DataGrid Styles -->
    <SolidColorBrush x:Key="DataGridBackground" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="DataGridForeground" Color="{StaticResource PrimaryTextColor}"/>
    <SolidColorBrush x:Key="DataGridHeaderBackground" Color="#0F172A"/>
    <SolidColorBrush x:Key="DataGridHeaderForeground" Color="{StaticResource PrimaryTextColor}"/>
    <SolidColorBrush x:Key="DataGridRowBackground" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="DataGridRowAlternateBackground" Color="{StaticResource SurfaceLightColor}"/>
    <SolidColorBrush x:Key="DataGridRowSelectedBackground" Color="{StaticResource PrimaryDarkColor}"/>
    <SolidColorBrush x:Key="DataGridGridLinesColor" Color="{StaticResource DividerColor}"/>
    
    <!-- Status Colors -->
    <SolidColorBrush x:Key="SuccessBrush" Color="#10B981"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#F59E0B"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#EF4444"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource PrimaryColor}"/>
    
    <!-- Chart Colors -->
    <SolidColorBrush x:Key="ChartBackground" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="ChartGridLines" Color="{StaticResource DividerColor}"/>
    <SolidColorBrush x:Key="ChartAxisColor" Color="{StaticResource SecondaryTextColor}"/>
    
</ResourceDictionary>
