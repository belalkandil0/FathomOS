<Window x:Class="FathomOS.Shell.Views.CertificateViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Certificate Viewer"
        Width="950" Height="750"
        MinHeight="600" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="#0D1117">

    <Window.Resources>
        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2EA043"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#196C2E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#21262D"/>
                                <Setter Property="Foreground" Value="#484F58"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#21262D"/>
            <Setter Property="Foreground" Value="#C9D1D9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30363D"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#161B22"/>
                                <Setter Property="Foreground" Value="#484F58"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#161B22" Padding="24,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x1F4DC;" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <TextBlock x:Name="txtCertTitle" Text="Certificate of Processing"
                                   FontSize="20" FontWeight="SemiBold" Foreground="#E6EDF3"/>
                    </StackPanel>
                    <TextBlock x:Name="txtCertId" Text="FOS-XX-0000-00000-XXXX"
                               FontSize="13" Foreground="#58A6FF" Margin="0,6,0,0"
                               FontFamily="Consolas"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border x:Name="badgeSynced" Background="#238636" CornerRadius="6"
                            Padding="14,8" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x2713;" Foreground="White" FontWeight="Bold" Margin="0,0,6,0"/>
                            <TextBlock Text="SYNCED" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                        </StackPanel>
                    </Border>
                    <Border x:Name="badgePending" Background="#D29922" CornerRadius="6"
                            Padding="14,8" Visibility="Visible">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x23F3;" Margin="0,0,6,0"/>
                            <TextBlock Text="PENDING SYNC" Foreground="#0D1117" FontWeight="SemiBold" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content - WebBrowser for HTML preview -->
        <Border Grid.Row="1" Margin="24,20" Background="#FFFFFF" CornerRadius="8"
                BorderBrush="#30363D" BorderThickness="1">
            <WebBrowser x:Name="webBrowser" />
        </Border>

        <!-- Footer with actions -->
        <Border Grid.Row="2" Background="#161B22" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Fill="#D29922" Margin="0,0,8,0"/>
                    <TextBlock x:Name="txtSyncStatus" Text="Pending server sync"
                               Foreground="#8B949E" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnPrint" Content="Print"
                            Style="{StaticResource SecondaryButton}"
                            Width="100" Margin="0,0,10,0"
                            Click="btnPrint_Click"/>
                    <Button x:Name="btnExport" Content="Export HTML"
                            Style="{StaticResource PrimaryButton}"
                            Width="120" Margin="0,0,10,0"
                            Click="btnExport_Click"/>
                    <Button x:Name="btnClose" Content="Close"
                            Style="{StaticResource SecondaryButton}"
                            Width="90"
                            Click="btnClose_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
