<Window x:Class="FathomOS.Shell.Views.CreateAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FathomOS - Create Administrator Account"
        Height="680" Width="550"
        MinHeight="600" MinWidth="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#0D1117">

    <Window.Resources>
        <!-- Text Box Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#58A6FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#58A6FF"/>
                            </Trigger>
                            <Trigger Property="Validation.HasError" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#F85149"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Password Box Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#58A6FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#58A6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2EA043"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#196C2E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#21262D"/>
                                <Setter Property="Foreground" Value="#484F58"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#21262D"/>
            <Setter Property="Foreground" Value="#C9D1D9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30363D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with License Info -->
        <Border Grid.Row="0" Background="#161B22" CornerRadius="8" Padding="20" Margin="0,0,0,24">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <Border Background="#238636" CornerRadius="12" Padding="6,4" Margin="0,0,10,0">
                        <TextBlock Text="Licensed" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                    </Border>
                    <TextBlock x:Name="ClientText" Text="License Active" Foreground="#8B949E" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock x:Name="EditionText" Text="FathomOS Professional" Foreground="#58A6FF" FontSize="16" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <!-- Account Creation Form -->
        <StackPanel Grid.Row="1">
            <!-- Title -->
            <TextBlock Text="Create Your Administrator Account" FontSize="22" FontWeight="SemiBold"
                       Foreground="#E6EDF3" Margin="0,0,0,8"/>
            <TextBlock Text="This account will have full access to all FathomOS features."
                       Foreground="#8B949E" FontSize="13" Margin="0,0,0,24"/>

            <!-- Form Fields -->
            <Border Background="#161B22" CornerRadius="8" Padding="24">
                <StackPanel>
                    <!-- Full Name -->
                    <TextBlock Text="Full Name" Foreground="#C9D1D9" FontSize="13" Margin="0,0,0,8"/>
                    <TextBox x:Name="FullNameInput"
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,0,20"
                             TextChanged="Input_TextChanged"/>

                    <!-- Email -->
                    <TextBlock Text="Email Address" Foreground="#C9D1D9" FontSize="13" Margin="0,0,0,8"/>
                    <TextBox x:Name="EmailInput"
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,0,20"
                             TextChanged="Input_TextChanged"/>

                    <!-- Username -->
                    <TextBlock Text="Username" Foreground="#C9D1D9" FontSize="13" Margin="0,0,0,8"/>
                    <TextBox x:Name="UsernameInput"
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,0,20"
                             TextChanged="Input_TextChanged"/>

                    <!-- Password -->
                    <TextBlock Text="Password" Foreground="#C9D1D9" FontSize="13" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PasswordInput"
                                 Style="{StaticResource ModernPasswordBox}"
                                 Margin="0,0,0,16"
                                 PasswordChanged="Password_Changed"/>

                    <!-- Password Requirements -->
                    <Border Background="#0D1117" CornerRadius="6" Padding="16,12" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Password Requirements:" Foreground="#8B949E" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <StackPanel x:Name="PasswordRequirements">
                                <TextBlock x:Name="ReqLength" Text="&#x2022; At least 8 characters" Foreground="#8B949E" FontSize="12" Margin="0,2"/>
                                <TextBlock x:Name="ReqUppercase" Text="&#x2022; At least one uppercase letter" Foreground="#8B949E" FontSize="12" Margin="0,2"/>
                                <TextBlock x:Name="ReqLowercase" Text="&#x2022; At least one lowercase letter" Foreground="#8B949E" FontSize="12" Margin="0,2"/>
                                <TextBlock x:Name="ReqNumber" Text="&#x2022; At least one number" Foreground="#8B949E" FontSize="12" Margin="0,2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Confirm Password -->
                    <TextBlock Text="Confirm Password" Foreground="#C9D1D9" FontSize="13" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="ConfirmPasswordInput"
                                 Style="{StaticResource ModernPasswordBox}"
                                 PasswordChanged="Password_Changed"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Status Message -->
        <Border x:Name="StatusBorder" Grid.Row="2" Background="#21262D" CornerRadius="6"
                Padding="16,12" Margin="0,20,0,0" Visibility="Collapsed" VerticalAlignment="Top">
            <TextBlock x:Name="StatusText" TextWrapping="Wrap" FontSize="13"/>
        </Border>

        <!-- Action Buttons -->
        <Grid Grid.Row="3" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Version Info -->
            <TextBlock Text="Administrator Account Setup" Foreground="#484F58" FontSize="11" VerticalAlignment="Center"/>

            <!-- Create Account Button -->
            <Button x:Name="CreateAccountButton" Grid.Column="1" Content="Create Account"
                    Style="{StaticResource PrimaryButton}"
                    Width="160" Margin="0,0,10,0"
                    IsEnabled="False"
                    Click="CreateAccount_Click"/>

            <!-- Cancel Button -->
            <Button Grid.Column="2" Content="Cancel"
                    Style="{StaticResource SecondaryButton}"
                    Width="90"
                    Click="Cancel_Click"/>
        </Grid>
    </Grid>
</Window>
