<mah:MetroWindow x:Class="FathomOS.Modules.SurveyLogbook.Views.ManualEntryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:models="clr-namespace:FathomOS.Modules.SurveyLogbook.Models"
        mc:Ignorable="d"
        Title="Add Manual Entry"
        Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}"
        BorderBrush="{DynamicResource AccentBrush}"
        BorderThickness="1"
        GlowBrush="{DynamicResource AccentBrush}"
        TitleCharacterCasing="Normal">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Entry Type -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Entry Type" Style="{DynamicResource FieldLabel}"/>
                    <ComboBox x:Name="EntryTypeCombo"
                              ItemsSource="{Binding EntryTypes}"
                              SelectedItem="{Binding SelectedEntryType}"
                              DisplayMemberPath="DisplayName"
                              Padding="10,8"/>
                </StackPanel>

                <!-- Date/Time -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Date" Style="{DynamicResource FieldLabel}"/>
                        <DatePicker SelectedDate="{Binding EntryDate}" Padding="10,8"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Time" Style="{DynamicResource FieldLabel}"/>
                        <mah:TimePicker SelectedDateTime="{Binding EntryTime}"
                                        PickerVisibility="HourMinute"
                                        Culture="en-GB"/>
                    </StackPanel>
                </Grid>

                <!-- Object/Source Name -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Object / Source Name" Style="{DynamicResource FieldLabel}"/>
                    <TextBox Text="{Binding ObjectName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource ModernTextBox}"
                             mah:TextBoxHelper.Watermark="e.g., ROV1, Vessel, Sensor"/>
                </StackPanel>

                <!-- Description -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Description / Comment" Style="{DynamicResource FieldLabel}"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource ModernTextBox}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             Height="100"
                             VerticalScrollBarVisibility="Auto"
                             mah:TextBoxHelper.Watermark="Enter event description..."/>
                </StackPanel>

                <!-- Position Information (Optional) -->
                <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,15" Padding="15">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <CheckBox x:Name="IncludePositionCheck" 
                                      Content="Include Position Data"
                                      IsChecked="{Binding IncludePosition}"
                                      Foreground="{DynamicResource TextPrimaryBrush}"/>
                        </StackPanel>

                        <Grid IsEnabled="{Binding IsChecked, ElementName=IncludePositionCheck}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,10,10">
                                <TextBlock Text="Easting" Style="{DynamicResource FieldLabel}"/>
                                <mah:NumericUpDown Value="{Binding Easting}"
                                                   StringFormat="F3"
                                                   NumericInputMode="Decimal"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,0,10">
                                <TextBlock Text="Northing" Style="{DynamicResource FieldLabel}"/>
                                <mah:NumericUpDown Value="{Binding Northing}"
                                                   StringFormat="F3"
                                                   NumericInputMode="Decimal"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,10,0">
                                <TextBlock Text="KP (km)" Style="{DynamicResource FieldLabel}"/>
                                <mah:NumericUpDown Value="{Binding Kp}"
                                                   StringFormat="F3"
                                                   NumericInputMode="Decimal"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Grid.Row="1">
                                <TextBlock Text="Depth (m)" Style="{DynamicResource FieldLabel}"/>
                                <mah:NumericUpDown Value="{Binding Depth}"
                                                   StringFormat="F2"
                                                   NumericInputMode="Decimal"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Border Grid.Row="1" 
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="0,15,0,0"
                Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton"
                        Content="Cancel"
                        Style="{DynamicResource SecondaryButton}"
                        Margin="0,0,10,0"
                        Width="100"
                        IsCancel="True"
                        Click="CancelButton_Click"/>
                
                <Button x:Name="AddButton"
                        Style="{DynamicResource PrimaryButton}"
                        Width="120"
                        IsDefault="True"
                        Click="AddButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Plus" Width="14" Height="14" Margin="0,0,8,0"/>
                        <TextBlock Text="Add Entry"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</mah:MetroWindow>
