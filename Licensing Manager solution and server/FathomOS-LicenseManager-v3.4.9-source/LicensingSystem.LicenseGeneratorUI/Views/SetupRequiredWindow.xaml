<Window x:Class="LicenseGeneratorUI.Views.SetupRequiredWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Server Setup Required"
        Height="600" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0D1117">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#0D1117"/>
        <SolidColorBrush x:Key="CardBrush" Color="#161B22"/>
        <SolidColorBrush x:Key="ElevatedBrush" Color="#21262D"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#30363D"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#58A6FF"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#3FB950"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#D29922"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#F85149"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#E6EDF3"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#8B949E"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#6E7681"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ElevatedBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource ElevatedBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#1A1E22"/>
                                <Setter Property="Foreground" Value="#6E7681"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2EA043"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Server Setup Required"
                       FontSize="26" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Create the first administrator account to begin using the License Manager."
                       FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Info Alert -->
        <Border Grid.Row="1" Background="#1F2937"
                BorderBrush="#3B82F6" BorderThickness="1"
                CornerRadius="8" Padding="16" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="i" FontSize="16" FontWeight="Bold" Foreground="#3B82F6"
                           VerticalAlignment="Top" Margin="0,0,12,0"
                           Width="20" TextAlignment="Center"/>
                <TextBlock TextWrapping="Wrap" Foreground="#D1D5DB" FontSize="13">
                    <Run Text="The license server at "/>
                    <Run x:Name="ServerUrlText" Text="localhost" FontWeight="SemiBold"/>
                    <Run Text=" requires initial setup. This wizard will create your administrator account."/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Form -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Email Address *" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" FontWeight="Medium" Margin="0,0,0,6"/>
                <TextBox x:Name="EmailInput" Style="{StaticResource ModernTextBox}"
                         TextChanged="Input_TextChanged"/>

                <TextBlock Text="Username *" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" FontWeight="Medium" Margin="0,16,0,6"/>
                <TextBox x:Name="UsernameInput" Style="{StaticResource ModernTextBox}"
                         TextChanged="Input_TextChanged"/>

                <TextBlock Text="Display Name (optional)" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" FontWeight="Medium" Margin="0,16,0,6"/>
                <TextBox x:Name="DisplayNameInput" Style="{StaticResource ModernTextBox}"/>

                <TextBlock Text="Password *" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" FontWeight="Medium" Margin="0,16,0,6"/>
                <PasswordBox x:Name="PasswordInput" Style="{StaticResource ModernPasswordBox}"
                             PasswordChanged="PasswordInput_Changed"/>

                <TextBlock x:Name="PasswordHint" FontSize="11" Foreground="{StaticResource TextMutedBrush}"
                           Margin="0,6,0,0" TextWrapping="Wrap"
                           Text="Minimum 12 characters, uppercase, lowercase, digit, and special character"/>

                <TextBlock Text="Confirm Password *" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" FontWeight="Medium" Margin="0,16,0,6"/>
                <PasswordBox x:Name="ConfirmPasswordInput" Style="{StaticResource ModernPasswordBox}"
                             PasswordChanged="ConfirmPasswordInput_Changed"/>

                <TextBlock x:Name="ConfirmHint" FontSize="11" Foreground="{StaticResource ErrorBrush}"
                           Margin="0,6,0,0" Visibility="Collapsed"/>

                <!-- Error message -->
                <Border x:Name="ErrorBorder" Background="#2D1B1B"
                        BorderBrush="{StaticResource ErrorBrush}" BorderThickness="1"
                        CornerRadius="6" Padding="14" Margin="0,20,0,0"
                        Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="{StaticResource ErrorBrush}"
                               TextWrapping="Wrap" FontSize="13"/>
                </Border>

                <!-- Success message -->
                <Border x:Name="SuccessBorder" Background="#1A2E1A"
                        BorderBrush="{StaticResource SuccessBrush}" BorderThickness="1"
                        CornerRadius="6" Padding="14" Margin="0,20,0,0"
                        Visibility="Collapsed">
                    <TextBlock x:Name="SuccessText" Foreground="{StaticResource SuccessBrush}"
                               TextWrapping="Wrap" FontSize="13"/>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="Cancel" Width="100" Style="{StaticResource SecondaryButton}"
                    Click="Cancel_Click"/>
            <Button x:Name="CompleteButton" Content="Complete Setup"
                    Width="150" Margin="12,0,0,0"
                    Style="{StaticResource PrimaryButton}"
                    Click="CompleteSetup_Click"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
